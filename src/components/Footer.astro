---
import SectionLayout from "../layouts/SectionLayout.astro";
import { logoBig } from "../assets";
import { contactInfo, idPages, socials } from "../data";
---

<SectionLayout id={idPages.contact} backgroundImage="bg-footer-pattern">
  <footer class="flex flex-col">
    <div class="flex flex-col md:flex-row justify-center items-center md:items-start">
      <div class="flex flex-col flex-1 justify-center items-center">
        <h4
          class="text-primary font-bold text-xl tracking-wider mb-6 mt-4 underline underline-offset-8"
        >
          Nuestras redes sociales
        </h4>
        <div class="flex items-center justify-items-center">
          {
            socials.map((social) => (
              <a href={social.url} target="_blank" class="mx-3">
                <img
                  class="w-12 cursor-pointer hover:animate-bounce my-0 mx-auto "
                  src={social.icon}
                  alt={social.title}
                />
              </a>
            ))
          }
        </div>
      </div>
      <div class="flex flex-col flex-1 justify-center items-center">
        <h4
          class="text-primary font-bold text-xl tracking-wider mb-6 mt-4 underline underline-offset-8"
        >
          Comunícate con nosotros
        </h4>
        <div class="flex flex-col items-center justify-center">
          <span
            class="text-secondary font-bold text-xl tracking-wider mb-6 text-center my-0 mx-auto"
          >
            <span class="mr-2">📞</span>{contactInfo.phones[0]}</span
          >
          <span
            class="text-secondary font-bold text-xl tracking-wider mb-6 text-center my-0 mx-auto"
          >
            <span class="mr-2">📞</span>{contactInfo.phones[1]}</span
          >
        </div>
      </div>
      <div class="flex flex-col flex-1 justify-center items-center">
        <img class="h-56" src={logoBig} alt="logo" />
      </div>
    </div>
    <hr class="h-px my-8 bg-gray-200 border-0 dark:bg-gray-700" />
    <div class="flex justify-center items-center">
      <span class="text-slate-800"
        >Hecho por <a
          class="text-slate-950 underline hover:no-underline"
          href="https://julandrod.github.io/"
          target="_blank">Julandrod</a
        > | 2023 🇨🇴</span
      >
    </div>
  </footer>
</SectionLayout>
