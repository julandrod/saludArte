---
import SectionLayout from "../layouts/SectionLayout.astro";
import {logoBig} from "../assets";
import { idPages, socials } from "../data";
---

<SectionLayout id={idPages.contact} backgroundImage="bg-footer-pattern">
  <footer class="flex flex-col">
    <div class="flex flex-col md:flex-row">
      <div class="flex flex-col flex-1 justify-center items-center">
        <h4 class="text-primary font-medium text-lg tracking-wider mb-6 mt-4">
          Nuestras redes sociales
        </h4>
        <div class="flex">
          {
            socials.map((social) => (
              <a href={social.url} target="_blank">
                <img
                  class="w-12 mr-4 cursor-pointer hover:scale-110"
                  src={social.icon}
                  alt={social.title}
                />
              </a>
            ))
          }
        </div>
      </div>
      <div class="flex flex-col flex-1 justify-center items-center">
        <h4 class="text-primary font-medium text-lg tracking-wider mb-6 mt-4">
          Comunícate con nosotros
        </h4>
        <p class="text-primary font-medium text-lg tracking-wider mb-6">
          <span class="mr-2">📞</span> 316 248 4028
        </p>
      </div>
      <div class="flex flex-col flex-1 justify-center items-center">
        <img class="h-56" src={logoBig} alt="logo" />
      </div>
    </div>
    <hr class="h-px my-8 bg-gray-200 border-0 dark:bg-gray-700" />
    <div class="flex justify-center items-center">
      <span class="text-slate-800"
        >Hecho por <a
          class="text-slate-950 underline hover:no-underline"
          href="https://julandrod.github.io/"
          target="_blank">Julandrod</a
        > | 2023 🇨🇴</span
      >
    </div>
  </footer>
</SectionLayout>
