---
import { logoBig, mapaUser } from "../assets";
import { idPages, services } from "../data";
import SectionLayout from "../layouts/SectionLayout.astro";
import SectionTitle from "./SectionTitle.astro";
import ServiceCard from "./ServiceCard.astro";
---

<SectionLayout id={idPages.services}>
  <SectionTitle title="Nuestros servicios" />
  <div
    class="grid grid-cols-1 md:grid-cols-2 gap-2 items-center justify-center"
  >
    {
      services.map((service) => (
        <ServiceCard
          title={service.title}
          description={service.description}
          image={service.image}
        />
      ))
    }
  </div>
  <div class="flex items-center justify-center">
    <div class="flex items-center justify-center w-4/6 flex-col lg:flex-row">
      <img class="flex-2 h-52" src={logoBig} alt="logo saludarte" />
      <div class="flex-1 flex flex-col justify-center items-center">
        <h2 class="text-zinc-900 text-5xl font-bold">Agenda tu cita</h2>
        <p class="text-zinc-900 text-4xl mt-6">🗓️ 316 248 4028</p>
      </div>
      <img class="flex-2 h-52" src={mapaUser} alt="usuario mapa" />
    </div>
  </div>
</SectionLayout>
